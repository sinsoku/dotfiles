常に日本語で返信すること。

## 環境

- シェルは zsh。
- ツールのバージョン管理は mise（bun / node / ruby など）。`ruby`・`bundle`・`node` を直接呼ぶと mise が解決する。
- リポジトリは ghq レイアウトで配置（`~/ghq/github.com/<owner>/<repo>`）。
- 主に Ruby / TypeScript のプロジェクトで作業する。
